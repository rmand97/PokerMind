info:
  name: /start_suite
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/api/start_suite"
  headers:
    - name: authorization
      value: "{{secret}}"
  body:
    type: json
    data: |-
      {
        "players": ["stine", "rolf"],
        "num_games": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
