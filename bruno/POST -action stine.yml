info:
  name: POST /action stine
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:4000/api/action
  headers:
    - name: authorization
      value: test-secret
  body:
    type: json
    data: |-
      {
        "player_id": "stine",
        "game_id": "e484675c-2886-4834-8db1-bb5a827cb57f-1",
        "action": "check"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
