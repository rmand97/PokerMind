info:
  name: POST /start_suite
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:4000/api/start_suite
  headers:
    - name: authorization
      value: test-secret
  body:
    type: json
    data: |-
      {
        "players": ["stine", "rolf"],
        "num_games": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
