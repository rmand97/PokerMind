info:
  name: /suites
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/suites"
  headers:
    - name: authorization
      value: "{{secret}}"
  body:
    type: json
    data: |-
      {
        "player_id": "stine",
        "suite_id": "91f92ffb-516a-4da7-aa2e-7c96848595b1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
